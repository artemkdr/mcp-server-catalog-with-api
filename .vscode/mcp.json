{
    "servers": {
        "catalog-with-api": {
            "type": "stdio",
            "command": "bun",
            "args": ["run", "${workspaceFolder}/src/index.ts"],            
            "env": {
                "NODE_ENV": "development"
            }
        }
    }
}